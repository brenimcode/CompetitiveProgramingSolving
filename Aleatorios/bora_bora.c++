#include <bits/stdc++.h>
using namespace std;

int main(){
    // Dado numero de jogadores, numero de cartas dada para cada jogador
    // e dada numero de cartas embaralhadas, determine o vencedor em cada partida.
    int p,m,n;
    while(cin >> p >> m >> n){
        if(p == 0 && p == m && m == n)
            return 0;
        
    }
}
