#include <bits/stdc++.h>

using namespace std;

typedef long long ll;


int32_t main(){
    ll n,k,i;

    string r;
    getline(cin,r);
    reverse(r.begin(),r.end());
    cout << r;

    return 0;
}
